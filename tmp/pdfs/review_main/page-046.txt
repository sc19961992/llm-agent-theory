J
自回归模型的递推下一TOKEN 表示：分布加权闭合与ΓN 判据
46
对确定性映射f : A →B，记其对应的通道为
Qf(a, b) := 1{f(a) = b}.
通道按从左到右的次序复合，例如KNQf 表示先按KN 转移再应用f。令
νN := (xN)∗µN.
对同型通道A, B : Xreach
N
⇝Z，定义分布加权距离
dνN (A, B) :=
X
x∈Xreach
N
νN(x) ∥A(x, ·) −B(x, ·)∥TV ,
∥p −r∥TV := 1
2
X
z
|p(z) −r(z)|.
该距离是νN 支撑上的平均逐行TV 距离；全状态最坏情形由正文的d∞度量。
J.2
三个假设
以下三个假设共同描述参考过程的递推表示、模型分布误差及该表示与完整状态的相容性。
假设T1（低相对状态数的递推下一token 表示）.
存在有限集合SN、满射sN : HN ↠SN、
读出核RN : SN ⇝VN 和确定性更新FN : SN × VN →SN，使所有相关扩展满足
sN(ha) = FN(sN(h), a),
(T1a)
并且
Eh∼µN ∥PN(· | h) −RN(sN(h), ·)∥TV ≤ηN.
(T1b)
相对于正文中事先固定的可辨微观基线BN，还要求
|SN|
|BN| ≤λN.
(T1c)
式(T1a) 提供闭合所需的递推结构；T1b 单独给出的下一token 读出误差只控制输出分布。
假设T2（总体下一token KL 界）.
以自然对数计算KL 散度，并假定
δN := Eh∼µN DKL

PN(· | h)
 bPN(· | h)

< ∞.
(T2)
这是声明分布上的总体额外交叉熵。从有限训练或测试误差得到式(T2) 需要相应的总体泛化
界。由于这里采用正向KL，有限性还要求对µN-几乎处处的历史满足
PN(· | h) ≪bPN(· | h).
因此，top-k 或nucleus 解码要求参考过程在全部被截断token 上赋零概率；贪心Dirac 解码要
求参考条件分布是与实际贪心解码相同的Dirac 分布。一般截断解码可改为对截断前模型分布
验证KL，再把解码变换造成的TV 误差单独计入。
