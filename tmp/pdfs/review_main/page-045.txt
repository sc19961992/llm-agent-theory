J
自回归模型的递推下一TOKEN 表示：分布加权闭合与ΓN 判据
45
即q∗RY 是PK-不变的、含常数且对逐点乘法封闭的观测代数。附录C 证明，在有限集合上这
类代数恰与分区对应。
因此，“宏观商空间”“闭合观测代数”和“交换方块”是同一有限精确结构的三种等价表
示。任务保真由任务因子化保证，压缩由相对状态数度量，近似最优值达到依赖候选集的有限
性或紧性，系统族满足ΓN →0 则依赖状态比例与闭合误差的跨规模联合控制。
J
自回归模型的递推下一token 表示：分布加权闭合与ΓN 判据
本节给出一个自包含的有限状态条件式定理。它适用于边界固定的自回归生成器，只使用
实际采样条件分布、完整Markov 状态和token 驱动的状态更新；Transformer[18] 只是可能的
实例之一，证明本身不依赖attention 或特定参数化。假设参考条件分布具有相对状态数受控的
递推下一token 表示，模型条件分布满足总体KL 界，且该表示与模型完整状态相容，定理据此
控制给定表示的分布加权下一token 误差与一步闭合误差。本附录不证明任何具体Transformer
满足这些假设，也不构成神经模型的经验验证；从这里到正文ΓN 仍需本附录最后明确列出的
最坏情形条件。
J.1
系统边界与统一记号
对每个规模指标N，固定模型参数、解码规则、有限词表VN、最大上下文长度与最大生成
长度。令HN 为带有位置、生成阶段和终止信息的有限历史集合；EOS 后的终止状态视为吸收
态。只考虑声明时域内的相关扩展ha ∈HN。令
PN( · | h),
bPN( · | h) ∈∆(VN)
分别为参考过程与模型在历史h 后的下一token 条件分布，并令µN ∈∆(HN) 为预先声明的
历史分布。这里bPN 表示经过温度、截断或其他固定解码变换后的实际采样分布；确定性解码
对应Dirac 分布。PN 在全部声明历史上有定义，结论覆盖µN 赋予质量的历史；自由生成到达
其他历史时需要相应rollout 分布上的附加界。
令Xreach
N
为可达的完整推理状态集，xN(h) ∈Xreach
N
为读取h 后的状态。完整状态包括使
下一步生成满足Markov 性所需的全部量；对Transformer，可包括历史、位置、KV cache、解
码器状态以及已纳入模型边界的环境状态。形式上，假定下式右端只依赖xN(h)：
Gnext
N
(xN(h), a) := bPN(a | h),
KN(xN(h), x′) :=
X
a∈VN
bPN(a | h) 1{xN(ha) = x′}.
若相同完整状态可能由多个历史表示，这一假定同时要求上述条件分布与token 后继状态保持
一致；把完整历史纳入状态即可保证它。
为与正文的复杂度基线比较，再把Xreach
N
作为有限观测–动力学呈现的原始状态空间，并
记其可辨微观商为
pN : Xreach
N
↠BN.
T1c 仅使用基数比|SN|/|BN|；读出qN 经BN 因子化以及任务保真属于本附录最后一节提升
为ΓN 证书时加入的条件。
